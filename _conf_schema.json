{
  "wegame_api_key": {
    "type": "string",
    "description": "WeGame作用域 API Key",
    "hint": "用于访问 API 的开发者密钥（可先使用公开key，加群等待前端完毕后订阅私key）",
    "default": ""
  },
  "api_base_url": {
    "type": "string",
    "description": "API 基础地址",
    "hint": "WeGame API 服务器地址",
    "default": "https://wegame.shallow.ink"
  },
  "render_timeout": {
    "type": "int",
    "description": "渲染超时时间",
    "hint": "图片渲染最大等待时间（毫秒）",
    "default": 30000
  },
  "help_prefix_display": {
    "type": "string",
    "description": "菜单页指令展示前缀",
    "hint": "仅用于帮助菜单图片中的命令展示，不影响实际命令注册；例如填写 9 后显示为 9洛克",
    "default": ""
  },
  "auto_refresh_enabled": {
    "type": "bool",
    "description": "启用自动刷新",
    "hint": "定时刷新所有用户凭证，非必要不要开启！服务端会自动刷新",
    "default": false
  },
  "auto_refresh_time": {
    "type": "list",
    "description": "自动刷新时间",
    "hint": "每天自动刷新的时间点（24 小时制）",
    "default": ["00:00", "12:00"],
    "items": {
      "type": "string"
    }
  },
  "auto_refresh_notify_group": {
    "type": "string",
    "description": "刷新结果通知 UMO",
    "hint": "自动刷新结果推送到此会话（填写完整的 UMO，如 default:GroupMessage:870543663）",
    "default": ""
  },
  "merchant_subscription_enabled": {
    "type": "bool",
    "description": "启用远行商人订阅推送",
    "hint": "开启后会周期检查远行商人商品并向已订阅群推送",
    "default": true
  },
  "merchant_subscription_items": {
    "type": "list",
    "description": "远行商人默认订阅商品",
    "hint": "群管理员订阅时默认监听这些商品名称，按名称精确匹配",
    "default": ["国王球", "棱镜球", "炫彩精灵蛋"],
    "items": {
      "type": "string"
    }
  },
  "merchant_check_cron": {
    "type": "string",
    "description": "远行商人检查计划",
    "hint": "Cron 表达式，默认每 5 分钟检查一次并按轮次去重推送",
    "default": "*/5 * * * *"
  }
}
